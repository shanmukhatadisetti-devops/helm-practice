apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    component: catalogue
    env: dev
spec:
  selector:
    project: roboshop
    component: catalogue
    env: dev
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  